h1 Package Details

table
  tr
    th Tracking #
    td!= package.trackingNumber ? package.trackingNumber : '<small>Not shipped</small>'
    th Status
    td!= status_label(package)
  tr
    th Container
    td= package.container.description
    th Dimensions
    td
      = dimensions(package.container.dimensions)
      != linkTo('edit', "#", {class:'editInline'})
  tr
    th Postage
    td
      = package.postage.amount.toCurrency()
      small= " " + package.postage.provider
    th Weight
    td
      = package.weight
      != linkTo('edit', "#", {class:'editInline'})
table
  tr
    th(colspan=2) Origin
    th(colspan=2) Destination
  tr
    td(colspan=2, style="padding:30px")
      = package.origin.name
      br
      small
        address!= address(package.origin.address, '<br />')
    td(colspan=2, style="padding:30px")
      = package.destination.name
      br
      small
        address!= address(package.destination.address, '<br />')

table
  tr
    th(colspan=2) History
  tr
    td Created
    td
      small ???
  - each event in package.history
    tr
      td= event.description
      td
        small= date(event.created)