h1= title + " (" + results.totalRecords + ")"

form.search(action="", method="get", style="overflow:hidden;")
  p(style="float:left;")
    input(type="search", placeholder="Keywords")
  p.statuses(style="float:right;")
    strong Status
    a(href="/orders") All
    a(href="/orders/open") Open
    a(href="/orders/processing") Processing
    
table
  thead
    tr
      th ID
      th Customer
      th.r.r(style="padding-right:40px;") Amount
      th Created
      th Status
      th
  tbody
    != partial('order', {collection: results.orders, as: "order"})
      