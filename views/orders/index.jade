h1= title

form.search(action="", method="get")
  p
    select(name="status")
      optgroup(label="Order Status")
        option - Any status -
        option New
        option Open
        option Completed
    input(type="search", placeholder="Keywords")
    input(type="submit", value="Search!")  

table
  thead
    tr
      th Order ID
      th Customer
      th Amount
      th Created
      th Status
  tbody
    != partial('order', {collection: orders, as: "order"})
      