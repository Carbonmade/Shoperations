- nav = {All: '/orders', Open: '/orders/open', Processing: '/orders/processing', Completed: '/orders/completed'}

- for(n in nav)
  - active = params.status === n || (!params.status && n === 'All')
  a(href=nav[n], class= active ? 'active' : 'inactive')= n